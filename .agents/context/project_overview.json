{
  "scout": {
    "project_name": "youtube-live-adapter",
    "tech_stack": [
      "python",
      "3.11",
      "httpx",
      "asyncio",
      "Neo-MoFox-plugin"
    ],
    "source_root": "src/",
    "modules": [
      {
        "path": "src/api.py",
        "description": "YouTube Inner Tube API 交互层（HTTP 请求构建与响应解析）",
        "estimated_files": 1
      },
      {
        "path": "src/client.py",
        "description": "HTTP 轮询客户端（自适应轮询间隔、连接管理）",
        "estimated_files": 1
      },
      {
        "path": "src/dispatcher.py",
        "description": "消息分发器（将 YouTube action dict 转换为 MessageEnvelope）",
        "estimated_files": 1
      },
      {
        "path": "src/sender.py",
        "description": "YouTube Data API v3 出站消息发送器（OAuth2 刷新令牌鉴权）",
        "estimated_files": 1
      },
      {
        "path": "src/currency.py",
        "description": "Super Chat 货币转换器（汇率 API 获取与 CNY 转换）",
        "estimated_files": 1
      },
      {
        "path": "plugin.py",
        "description": "插件入口与适配器生命周期管理（注册、加载、启停、重连）",
        "estimated_files": 1
      },
      {
        "path": "config.py",
        "description": "配置定义（BaseConfig + @config_section，含插件/Youtube/连接/过滤/SC/出站配置）",
        "estimated_files": 1
      }
    ],
    "config_files": [
      "config.py",
      "manifest.json",
      "pyproject.toml"
    ],
    "test_directory": "tests/",
    "build_system": "setuptools（pyproject.toml）",
    "virtual_environment": "未检测到",
    "key_files": [
      "README.md",
      "pyproject.toml",
      "manifest.json",
      "plugin.py",
      "config.py",
      "__init__.py"
    ]
  },
  "gitignore": {
    "rules_count": 4,
    "ignored_modules": []
  },
  "modules": [
    {
      "path": "src/api.py",
      "report": {
        "module_path": "src/api.py",
        "purpose": "YouTube Inner Tube API 的 HTTP 请求构建与响应解析层。封装了多客户端配置（WEB / iOS / TV）及 fallback 策略，负责获取直播间的初始 continuation token、轮询直播消息，并支持代理配置。该模块是 YouTube 直播聊天数据抓取的底层通信核心。",
        "key_classes": [
          {
            "name": "YouTubeInnerTubeAPI",
            "file": "src/api.py",
            "description": "YouTube Inner Tube API HTTP 客户端。使用 httpx.AsyncClient 发起异步请求，支持多客户端 fallback、代理和超时配置。提供获取初始 continuation token 和轮询直播消息的公共接口。"
          },
          {
            "name": "YouTubeApiError",
            "file": "src/api.py",
            "description": "YouTube Inner Tube API 请求错误的基类异常。"
          },
          {
            "name": "TokenExpiredError",
            "file": "src/api.py",
            "description": "Continuation token 已失效时抛出的异常，需要重新获取。"
          }
        ],
        "key_functions": [
          {
            "name": "get_initial_continuation",
            "file": "src/api.py",
            "description": "获取直播间的初始 continuation token。优先通过 Inner Tube API 请求获取，失败后回退到抓取视频页面 HTML 解析 ytInitialData。"
          },
          {
            "name": "get_live_chat_messages",
            "file": "src/api.py",
            "description": "轮询直播间消息。传入 continuation token，返回消息 actions 列表和新的 continuation token。当 token 过期时抛出 TokenExpiredError。"
          },
          {
            "name": "_get_continuation_via_api",
            "file": "src/api.py",
            "description": "通过 Inner Tube API 直接请求获取初始 continuation token，支持多路径解析响应。"
          },
          {
            "name": "_get_continuation_via_html",
            "file": "src/api.py",
            "description": "通过抓取视频页面 HTML 并解析 ytInitialData JSON 来获取 continuation token，作为 API 方式失败后的 fallback。"
          },
          {
            "name": "_build_init_payload",
            "file": "src/api.py",
            "description": "构建获取初始数据的 Inner Tube API 请求体（包含 videoId 和 client context）。"
          },
          {
            "name": "_build_poll_payload",
            "file": "src/api.py",
            "description": "构建轮询消息的 Inner Tube API 请求体（包含 continuation token 和 client context）。"
          },
          {
            "name": "_post",
            "file": "src/api.py",
            "description": "发送 POST 请求到 Inner Tube API 端点，根据客户端配置动态设置请求头，返回解析后的 JSON 响应。"
          },
          {
            "name": "_extract_continuation_token",
            "file": "src/api.py",
            "description": "从 YouTube 响应的 continuations 数组中提取 continuation token，支持 liveChatContinuationToken、timedContinuationData 和 invalidationContinuationData 三种格式。"
          }
        ],
        "dependencies": [
          "httpx",
          "src.kernel.logger"
        ],
        "patterns": [
          "策略模式用于多客户端选择（WEB / iOS / TV）及自动 fallback",
          "模板方法模式：请求构建（_build_init_payload / _build_poll_payload）与响应解析（_extract_continuation_token）分离",
          "防御式编程：多路径尝试解析 YouTube 不稳定的响应结构",
          "异步 IO：基于 httpx.AsyncClient 的全异步设计"
        ],
        "public_api": [
          "CLIENT_CONFIGS",
          "TokenExpiredError",
          "YouTubeApiError",
          "YouTubeInnerTubeAPI"
        ],
        "summary": "src/api.py 是 YouTube 直播适配器的底层 HTTP 通信层，核心职责是与 YouTube Inner Tube API 交互。模块定义了 `YouTubeInnerTubeAPI` 类，它封装了多客户端配置（WEB、iOS、TV）和自动 fallback 机制，以应对 YouTube 反爬策略的变化。主要提供两个公共接口：`get_initial_continuation` 用于获取直播间的初始 continuation token（支持 API 和 HTML 解析两种策略），以及 `get_live_chat_messages` 用于轮询直播消息。模块内部通过 `_build_init_payload`、`_build_poll_payload` 等方法构建符合 Inner Tube API 格式的请求体，并通过 `_extract_continuation_token` 从复杂的响应结构中提取下一轮请求所需的 token。设计上采用全异步架构（httpx.AsyncClient），支持代理和超时配置，异常处理区分了 `TokenExpiredError` 和通用的 `YouTubeApiError`，便于上层进行重试和恢复。"
      },
      "success": true
    },
    {
      "path": "src/client.py",
      "report": "",
      "success": true
    },
    {
      "path": "src/dispatcher.py",
      "report": {
        "module_path": "src/dispatcher.py",
        "purpose": "YouTube Live Chat 消息分发器，负责将 YouTube Inner Tube API 返回的原始 action dict 解析并转换为标准化的 MessageEnvelope 对象。支持普通弹幕、Super Chat、Super Sticker、会员礼物、会员里程碑等多种消息类型，同时提供消息去重、内容过滤、货币转换等增强功能。",
        "key_classes": [
          {
            "name": "YouTubeMessageDispatcher",
            "file": "src/dispatcher.py",
            "description": "核心分发器类，接收 YouTube action dict，根据 renderer 类型路由到对应的构建方法，最终输出 MessageEnvelope。支持消息去重、emoji/hashtag 过滤、长度截断、货币转换等配置。"
          }
        ],
        "key_functions": [
          {
            "name": "YouTubeMessageDispatcher.dispatch",
            "file": "src/dispatcher.py",
            "description": "入口方法，将单个 action dict 转换为 MessageEnvelope 或 None。处理 addChatItemAction 及其内部 item 的分发。"
          },
          {
            "name": "YouTubeMessageDispatcher._dispatch_item",
            "file": "src/dispatcher.py",
            "description": "根据 item 中的 renderer key（如 liveChatTextMessageRenderer）分发到对应的 _build_*_envelope 方法。"
          },
          {
            "name": "YouTubeMessageDispatcher._build_text_envelope",
            "file": "src/dispatcher.py",
            "description": "构建普通弹幕消息的 MessageEnvelope。"
          },
          {
            "name": "YouTubeMessageDispatcher._build_super_chat_envelope",
            "file": "src/dispatcher.py",
            "description": "构建 Super Chat 消息的 MessageEnvelope，支持异步货币转换（外币 → CNY）。"
          },
          {
            "name": "YouTubeMessageDispatcher._build_super_sticker_envelope",
            "file": "src/dispatcher.py",
            "description": "构建 Super Sticker 消息的 MessageEnvelope。"
          },
          {
            "name": "YouTubeMessageDispatcher._build_gift_purchase_envelope",
            "file": "src/dispatcher.py",
            "description": "构建会员礼物购买消息的 MessageEnvelope。"
          },
          {
            "name": "YouTubeMessageDispatcher._build_gift_redemption_envelope",
            "file": "src/dispatcher.py",
            "description": "构建会员礼物兑换消息的 MessageEnvelope。"
          },
          {
            "name": "YouTubeMessageDispatcher._build_milestone_envelope",
            "file": "src/dispatcher.py",
            "description": "构建会员里程碑消息的 MessageEnvelope。"
          },
          {
            "name": "YouTubeMessageDispatcher._build_paid_sticker_envelope",
            "file": "src/dispatcher.py",
            "description": "构建付费 Sticker 消息的 MessageEnvelope。"
          },
          {
            "name": "YouTubeMessageDispatcher._build_gift_announcement_envelope",
            "file": "src/dispatcher.py",
            "description": "构建会员礼物购买公告消息的 MessageEnvelope。"
          },
          {
            "name": "YouTubeMessageDispatcher._extract_nickname",
            "file": "src/dispatcher.py",
            "description": "从 renderer 中提取用户昵称（处理 authorName/simpleText 结构）。"
          },
          {
            "name": "YouTubeMessageDispatcher._extract_message_text",
            "file": "src/dispatcher.py",
            "description": "从 renderer 中提取消息文本，拼接 runs 数组或取 simpleText。"
          },
          {
            "name": "YouTubeMessageDispatcher._apply_filters",
            "file": "src/dispatcher.py",
            "description": "应用消息过滤规则：移除 emoji、移除 hashtag、截断超长消息。"
          },
          {
            "name": "YouTubeMessageDispatcher._is_duplicate",
            "file": "src/dispatcher.py",
            "description": "基于消息 ID 的去重检查，使用 maxlen=200 的 deque 缓存最近消息 ID。"
          },
          {
            "name": "YouTubeMessageDispatcher._inject_source_into_extra",
            "file": "src/dispatcher.py",
            "description": "将 source_platform 和 source_room_id 注入 message_info.extra，供下游 MessageConverter 使用。"
          }
        ],
        "dependencies": [
          "mofox_wire.MessageBuilder",
          "mofox_wire.MessageEnvelope",
          "mofox_wire.types.UserRole",
          "src.currency.CurrencyConverter",
          "src.kernel.logger.get_logger"
        ],
        "patterns": [
          "策略模式（Strategy Pattern）：根据 renderer 类型动态选择对应的 _build_*_envelope 构建方法",
          "Builder 模式：使用 MessageBuilder 链式 API 构建 MessageEnvelope（.direction().platform().text()...）",
          "模板方法模式：各消息类型的构建流程高度一致（提取字段 → 过滤 → builder 组装 → 注入 additional_config）",
          "常量共享设计：PLATFORM='live'、LIVE_VIRTUAL_GROUP_ID='live_room' 与 bilibili_live_adapter 共享，确保 anima_chatter 能统一识别为 vtb_live 模式"
        ],
        "public_api": [
          "YouTubeMessageDispatcher",
          "PLATFORM",
          "SOURCE_PLATFORM",
          "LIVE_VIRTUAL_GROUP_ID"
        ],
        "summary": "YouTubeMessageDispatcher 是 youtube-live-adapter 的核心组件，承担将 YouTube 原始直播聊天数据转换为标准化 MessageEnvelope 的职责。它通过 dispatch() 方法接收 Inner Tube API 返回的 action dict，识别其中的 renderer 类型（如 liveChatTextMessageRenderer、liveChatSuperChatRenderer 等），然后调用对应的私有构建方法生成 MessageEnvelope。构建过程中，分发器会统一注入平台标识（platform='live'、source_platform='youtube_live'）、群组信息、用户角色（SC/会员映射为 OPERATOR，普通用户为 MEMBER）以及时间戳等元数据。此外，模块还集成了消息去重（基于 message_id 的 LRU 缓存）、内容过滤（emoji/hashtag/长度截断）和货币转换（Super Chat 外币金额异步转换为 CNY）等增强功能，确保输出数据的质量和一致性。设计上与 bilibili_live_adapter 保持对齐，使用相同的 PLATFORM 和 LIVE_VIRTUAL_GROUP_ID 常量，使下游的 anima_chatter 和 stream_manager 能够无缝兼容多平台直播消息源。"
      },
      "success": true
    },
    {
      "path": "src/sender.py",
      "report": "",
      "success": true
    },
    {
      "path": "src/currency.py",
      "report": {
        "module_path": "src/currency.py",
        "purpose": "YouTube Live Adapter 的异步货币转换器，专门用于将 Super Chat 的外币金额（如 USD、JPY、TWD 等）转换为 CNY（人民币）。通过外部汇率 API 获取实时汇率，内置 TTL 缓存机制以减少 API 请求频率，并在转换失败时优雅降级（返回 None），确保不影响主流程。",
        "key_classes": [
          {
            "name": "CurrencyConverter",
            "file": "currency.py",
            "description": "异步货币转换器，使用 httpx.AsyncClient 从汇率 API 获取实时汇率，支持 TTL 缓存（默认 1 小时）。核心方法 convert_to_cny() 将外币金额转换为 CNY，转换失败时返回 None。"
          }
        ],
        "key_functions": [
          {
            "name": "CurrencyConverter.__init__",
            "description": "初始化转换器，支持自定义 api_url（默认 exchangerate-api.com）和 cache_ttl（默认 3600 秒）。创建独立的 httpx.AsyncClient 实例。"
          },
          {
            "name": "CurrencyConverter._fetch_rates",
            "description": "从汇率 API 异步获取最新汇率，解析 JSON 中的 base 货币和 rates 字典，更新内部缓存状态。"
          },
          {
            "name": "CurrencyConverter._get_rates",
            "description": "获取汇率字典，缓存有效时直接返回，过期时自动刷新。刷新失败时返回过期缓存或空字典，实现 graceful degradation。"
          },
          {
            "name": "CurrencyConverter.convert_to_cny",
            "description": "将指定外币金额转换为 CNY。公式：amount * (rates['CNY'] / rates[currency])。支持 CNY 直接返回、除零保护、缺失汇率保护。"
          },
          {
            "name": "CurrencyConverter.aclose",
            "description": "关闭内部 httpx.AsyncClient，释放资源。应在转换器生命周期结束时调用。"
          }
        ],
        "dependencies": [
          "httpx",
          "src.kernel.logger"
        ],
        "patterns": [
          "依赖注入：CurrencyConverter 通过构造函数参数注入到 YouTubeMessageDispatcher，解耦汇率转换与消息分发逻辑",
          "TTL 缓存模式：使用 _rates、_cache_time 和 _cache_ttl 实现简单的内存缓存，避免频繁请求外部 API",
          "Graceful Degradation：_get_rates 在 API 请求失败时返回过期缓存；convert_to_cny 在汇率缺失时返回 None，均不影响主流程",
          "独立客户端：CurrencyConverter 拥有独立的 httpx.AsyncClient，不与 API 客户端共享，避免生命周期耦合"
        ],
        "public_api": [
          "CurrencyConverter"
        ],
        "summary": "src/currency.py 是 YouTube Live Adapter 中专为 Super Chat 设计的货币转换模块。其核心类 CurrencyConverter 通过异步 HTTP 请求从 exchangerate-api.com 等汇率 API 获取实时汇率，并利用简单的 TTL 缓存（默认 1 小时）减少 API 调用频率。模块采用依赖注入模式，由 YouTubeMessageDispatcher 在初始化时传入；在 _build_super_chat_envelope 方法中，Super Chat 的原始外币金额（如 USD、JPY）会被自动转换为 CNY，结果写入 additional_config['superchat_amount_cny']。设计上强调容错性：API 失败时返回过期缓存，汇率缺失或转换异常时返回 None，确保不会阻塞或中断直播消息的主分发流程。模块还维护独立的 httpx 客户端生命周期，通过 aclose() 方法显式释放资源。"
      },
      "success": true
    },
    {
      "path": "plugin.py",
      "report": "",
      "success": true
    },
    {
      "path": "config.py",
      "report": {
        "module_path": "config.py",
        "purpose": "YouTube Live Adapter 的配置定义模块，采用 BaseConfig + @config_section 声明式配置模式，定义了插件开关、YouTube 连接、轮询重连、消息过滤、Super Chat 货币转换、出站消息等六大配置分区。配置类 YouTubeLiveAdapterConfig 作为插件与 Neo-MoFox 框架之间的配置契约，支持 TOML 文件持久化与运行时验证。",
        "key_classes": [
          {
            "name": "YouTubeLiveAdapterConfig",
            "file": "config.py",
            "description": "配置根类，继承 BaseConfig，包含 6 个配置分区字段（plugin/youtube/connection/filter/superchat/outbound），config_name='config'，config_description='YouTube 直播间弹幕接入适配器配置'"
          },
          {
            "name": "PluginSection",
            "file": "config.py",
            "description": "插件设置分区（@config_section('plugin')），包含 enabled（是否启用适配器）和 debug_mode（调试模式输出原始数据）"
          },
          {
            "name": "YouTubeSection",
            "file": "config.py",
            "description": "YouTube 连接分区（@config_section('youtube')），包含 video_id（必填）、language（界面语言）、proxy_url（HTTP/SOCKS5 代理）、client_name（Inner Tube API 客户端标识：WEB/IOS/TV）"
          },
          {
            "name": "ConnectionSection",
            "file": "config.py",
            "description": "轮询与重连分区（@config_section('connection')），包含 poll_interval、max_poll_interval、auto_reconnect、reconnect_initial_delay、reconnect_max_delay、reconnect_backoff_multiplier、request_timeout 等自适应轮询与指数退避参数"
          },
          {
            "name": "FilterSection",
            "file": "config.py",
            "description": "消息过滤分区（@config_section('filter')），包含 filter_emoji（过滤 emoji）、remove_hashtags（移除 hashtag）、max_message_length（最大长度截断）、ignored_message_types（忽略的消息类型列表）"
          },
          {
            "name": "SuperChatSection",
            "file": "config.py",
            "description": "Super Chat 货币转换分区（@config_section('superchat')），包含 enable_currency_conversion（是否启用 SC 金额转 CNY）和 exchange_rate_api_url（汇率 API 地址）"
          },
          {
            "name": "OutboundSection",
            "file": "config.py",
            "description": "出站消息分区（@config_section('outbound')），包含 enabled（是否启用出站）、client_id、client_secret、refresh_token（OAuth2 凭证三件套）、live_chat_id（可选，留空自动获取）。出入站绑定约束：outbound.enabled 必须为 True 且 OAuth2 凭证完整，适配器才启动。"
          }
        ],
        "key_functions": [],
        "dependencies": [
          "src.core.components.base.config (BaseConfig, Field, SectionBase, config_section)"
        ],
        "patterns": [
          "声明式配置模式：继承 BaseConfig + @config_section 装饰器定义配置分区，与 bilibili_live_adapter 保持一致",
          "分区化配置设计：每个逻辑域（plugin/youtube/connection/filter/superchat/outbound）独立为 SectionBase 子类，通过 Field 声明字段类型、默认值、描述、验证约束（ge/le/choices/input_type）",
          "出入站绑定约束：outbound.enabled 与 OAuth2 凭证的联合校验在 plugin.py 的 on_adapter_loaded 中执行，config.py 仅定义字段结构",
          "配置与运行时解耦：config.py 纯数据定义，无业务逻辑；plugin.py 通过 _get_config() 获取实例并驱动各组件（API/Dispatcher/Sender/Client）初始化"
        ],
        "public_api": [
          "YouTubeLiveAdapterConfig"
        ],
        "summary": "config.py 是 YouTube Live Adapter 的配置定义核心，采用 Neo-MoFox 框架统一的 BaseConfig + @config_section 声明式配置模式。模块定义了 YouTubeLiveAdapterConfig 根类，内含 6 个配置分区（PluginSection、YouTubeSection、ConnectionSection、FilterSection、SuperChatSection、OutboundSection），覆盖插件开关、YouTube 连接参数、轮询重连策略、消息过滤规则、SC 货币转换、出站 OAuth2 凭证等完整配置域。每个字段通过 Field 声明默认值、描述、验证约束（如 ge/le/choices），支持 TOML 文件持久化与框架级配置 UI 渲染。配置设计与 bilibili_live_adapter 保持一致，体现了跨插件配置标准化。运行时上，plugin.py 在 on_adapter_loaded 中读取配置并校验出入站绑定约束（outbound.enabled + OAuth2 凭证完整性），随后将各配置项注入 API、Dispatcher、Sender、Client 等组件，实现配置与业务逻辑的清晰解耦。"
      },
      "success": true
    }
  ],
  "virtual_environment": "未检测到"
}