# 重写 README.md 为详细教程式文档

> Created: 2026-06-13 03:20:46

# 重写 README.md 为详细教程式文档

## 目标
将现有的简略 README.md 重写为事无巨细的教程式文档，让完全不了解这个插件的用户也能一步步上手。

## 内容大纲

### 1. 项目简介
- 一句话说清楚这个插件干什么
- 核心特性列表（弹幕、SC、Super Sticker、会员礼物、自适应轮询、自动重连、代理、去重、货币转换）

### 2. 前置要求
- Neo-MoFox 版本要求（min_core_version: 1.2.0-alpha）
- Python 版本（>=3.11）
- 依赖：httpx>=0.27.0
- 网络要求（中国大陆需要代理才能访问 YouTube）

### 3. 安装步骤
- 插件文件放置位置
- 依赖安装

### 4. 配置详解（最核心部分）
- 配置文件路径：`config/youtube_live_adapter.toml`
- 逐个 section 详细解释，每个字段都说明：
  - 字段名
  - 类型
  - 默认值
  - 取值范围
  - 作用说明
  - 示例

  Sections:
  - `[plugin]` - enabled, debug_mode
  - `[youtube]` - video_id（重点：怎么从URL提取）, language, proxy_url, client_name
  - `[connection]` - poll_interval, max_poll_interval, auto_reconnect, reconnect_initial_delay, reconnect_max_delay, reconnect_backoff_multiplier, request_timeout
  - `[filter]` - filter_emoji, remove_hashtags, max_message_length, ignored_message_types
  - `[superchat]` - enable_currency_conversion, exchange_rate_api_url

### 5. 快速开始（Step by Step）
- Step 1: 获取直播 video_id（附图说明 URL 格式）
- Step 2: 配置代理（如需要）
- Step 3: 编辑配置文件
- Step 4: 启动插件
- Step 5: 验证是否正常工作

### 6. 支持的消息类型
- 列出所有支持的消息类型及对应的 YouTube renderer
- 普通弹幕、Super Chat、Super Sticker、会员礼物购买、会员礼物兑换、会员里程碑、付费贴纸

### 7. client_name 选择指南
- WEB / IOS / TV 的区别
- 推荐选择及原因

### 8. 代理配置说明
- HTTP 代理格式
- SOCKS5 代理格式
- 常见代理软件配置示例

### 9. SC 货币转换
- 启用方式
- 汇率 API 说明
- 自定义汇率 API

### 10. 故障排查
- 常见问题及解决方案
  - 连接不上 / 获取不到 token
  - 消息收不到
  - Token 频繁过期
  - 代理配置问题

### 11. 技术说明
- Inner Tube API 原理
- 轮询机制说明
- 重连策略说明
- 消息去重机制

### 12. 项目结构
- 文件树 + 每个文件的简要说明

## 验收标准
- README 覆盖所有配置字段，无遗漏
- 快速开始步骤可操作、无歧义
- 故障排查覆盖常见场景
- 文档风格清晰、友好，适合新手阅读
